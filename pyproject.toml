[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hello-app"
version = "0.1.0"
description = "Minimal Hello World CLI"
requires-python = ">=3.8"
